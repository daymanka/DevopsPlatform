
server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/devopsdb
    username: devops
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
    properties:
      hibernate:
        format_sql: true